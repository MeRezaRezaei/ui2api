# Raw Verbatim — Original Words (spelling/grammar corrected only)

This is the authoritative source for the V1 Gate requirement. Spelling/grammar corrected
only. Nothing re-worded, nothing reordered, nothing added or removed. The presentation copy
in `../VERBATIM.md` is NOT the source of truth — this is.

---

## The requirement (as spoken)

i think the way that the login works is not what i need in fact its too hard for end user
this tool has a lot of use cases and a lot of poeple who might actaully install it
nassassirily does not have too many skills to work with it so from two day agao that we
finally get one correct answer from gemini i am continusly thinking how i can make it more
simple and for linux there is a command called xhost + and it lets one scrren to be used
from other users and since for our work we are making a user name ui2api we should also
preserve the option for handling our logins this way simply by hiting it the only thing that
the user must do is to simply use the browsr and login to what ever accoount it wants the
regular way but the only diff is the command has been relaxsed the lock of display and data
ill be stored in the ui2api user not the current user that is using it this way is the most
realiable way to do it also there should be more options for this since chrome stores data
of local storage and cookie for anything just base on site domain so our tool simply can
scan the linux of our user and find out any site tha its info is exists amont any profile of
chrome that can be found in the os so its only amatter of check box for indexing and
inserting what ever our user wants to store so another thing is users are not nassaassirily
using one set of login or local storage one user might have several gemini accounts and
wwants to use all of them so its logical tha we storre the data and site in spicific
situation related to specific auth info email or what ever the site functionality posses
this way we can grant users abilty to use any account they want for this or even agrigate
them as they want or even for feuture develpment not now the ablity to use cross site data
since while we have all the functionality as api we can orcahstrate them as needed for manay
reasons one person may need. and this is the exact thing i think i need before calling the
first version of this app done and add this to verbatim and just like the reast of verbatims
do as i said in verbatim and read the verbatim each time the session capmpacts until the
goal works in real world end to end