{
  "capability": "claude_chat",
  "site": "claude",
  "flow": "ui-path (ChatDriver) — composer insertText + Enter, streamed answer",
  "steps": [
    { "step": 1, "action": "open", "url": "https://claude.ai", "note": "session snapshot injected (data/claude.ai/.session) — pending first live capture" },
    { "step": 2, "action": "new-chat", "onlyWhen": "args.newChat", "note": "jittered 600-1300ms human dwell" },
    { "step": 3, "action": "type", "input": "args.prompt", "note": "trusted keyboard.insertText" },
    { "step": 4, "action": "send", "note": "Enter (keyEnter strategy), 50-200ms gate" },
    { "step": 5, "action": "read", "selector": "[data-testid=\"assistant-message\"]", "until": "stable 2500ms", "timeoutMs": 90000, "note": "UNVERIFIED candidate selector — tune after first live capture" }
  ],
  "output": { "answer": "string", "chunkCount": "number", "doneReason": "stable|timeout|empty" },
  "rpc": "no wire endpoints documented for claude in capabilities/provider-catalog.md (auth-only entry: cookie sessionKey); the UI path drives the site's own JS so no over-the-wire reconstruction is needed — endpoint shapes to be recorded on first live capture",
  "stealth": "real session, trusted inputs, site's own send handler — indistinguishable from the user typing",
  "sessionLock": "session.lock.json"
}